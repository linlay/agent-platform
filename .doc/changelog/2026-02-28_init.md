# 2026-02-28 初始化文档

## 变更类型
- 新增 `.doc/` 全量文档体系（架构/API/后端/前端集成/术语/变更记录）。

## 覆盖范围
- 覆盖 `/api/ap/*` 公开契约与 SSE 事件约束。
- 覆盖 ONESHOT/REACT/PLAN_EXECUTE 编排逻辑。
- 覆盖 tool/skill/model/viewport 注册与热加载。
- 覆盖 JWT + chat image token 鉴权链。
- 覆盖 SQLite `CHATS` + JSONL `chats/*.json` 存储设计。

## 事实来源
- `CLAUDE.md`
- 项目主说明文档
- `src/main/java/**`
- `src/test/java/**`
- 运行时资源目录：`agents/models/tools/skills/viewports`

## 维护规则
1. 契约变更先改 `.doc/api/*`，再改代码。
2. 编排与模式变更先改 `.doc/backend/*`。
3. 若实现与文档冲突，先落 `[DOC-GAP]` 并在本目录记录修复。
