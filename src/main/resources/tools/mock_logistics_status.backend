{
  "tools": [
    {
      "type": "function",
      "name": "mock_logistics_status",
      "description": "[MOCK] 根据 trackingNo 查询物流状态（伪造数据）。",
      "prompt": "若需要 viewport 渲染，最终输出请使用 viewport 代码块并填写 type=html, key=show_logistics_status；JSON 直接使用工具返回结果对象。",
      "parameters": {
        "type": "object",
        "properties": {
          "trackingNo": {
            "type": "string",
            "description": "物流单号，例如 SF123456789CN"
          },
          "carrier": {
            "type": "string",
            "description": "物流公司，可选，例如 SF Express"
          }
        },
        "required": [
          "trackingNo"
        ],
        "additionalProperties": false
      }
    }
  ]
}
