name: plan_get_tasks
label: 读取任务
description: Get the current ordered plan snapshot. At most one task can be in_progress; currentTaskId identifies it when present. If no tasks exist, returns an empty plan snapshot.
clientVisible: true
inputSchema:
  type: object
  properties: {}
  additionalProperties: false
