# Bash 默认预设：适合本地联调，默认启用完整 shell 语法；如需收紧可显式关闭 shell-features-enabled。
# Default bash preset: suitable for local debugging with full shell syntax enabled by default; turn shell-features-enabled off to return to strict mode.
working-directory: .
allowed-paths: ., /tmp
allowed-commands: ls,pwd,cat,head,tail,top,free,df,git,rg,find,echo,printf,sed,awk,grep,wc,sort,uniq,tr,cut,xargs,cd,stat,file,du,test,which,mkdir,touch,cp,mv,rm,ln,chmod,env,date,bash,sh,make,go,npm,yarn,pnpm,node,python,python3,pip,curl,wget
path-checked-commands: ls,cat,head,tail,git,rg,find
path-check-bypass-commands: git
shell-features-enabled: true
shell-executable: bash
shell-timeout-ms: 30000
max-command-chars: 16000

# Bash 收紧预设：如需回到 strict mode，可保留完整白名单但关闭 shell features。
# Strict-mode preset: keep a broader allowlist but disable shell features when operators need tighter control.
# working-directory: .
# allowed-paths: ., /tmp, /Users/you/Project, /Users/you/Server
# allowed-commands: ls,pwd,cat,head,tail,top,free,df,git,rg,find,echo,printf,sed,awk,grep,wc,sort,uniq,tr,cut,xargs,cd,stat,file,du,test,which,mkdir,touch,cp,mv,rm,ln,chmod,env,date,bash,sh,make,go,npm,yarn,pnpm,node,python,python3,pip,curl,wget
# path-checked-commands: ls,cat,head,tail,du,git,rg,find,grep,wc,sed,awk,cut,sort,uniq,tr,xargs,mkdir,cp,mv,touch,echo
# path-check-bypass-commands: git,curl
# shell-features-enabled: false
# shell-executable: bash
# shell-timeout-ms: 20000
# max-command-chars: 24000

# Bash 危险全开预设（Dangerous Open）：允许任意命令与任意路径，仅适合完全信任调用方的隔离环境。
# Bash dangerous-open preset: arbitrary commands and paths; only for isolated environments where the caller is fully trusted.
# working-directory: .
# allowed-paths: /
# allowed-commands: '*'
# path-checked-commands: '*'
# path-check-bypass-commands: '*'
# shell-features-enabled: true
# shell-executable: bash
# shell-timeout-ms: 30000
# max-command-chars: 32000
