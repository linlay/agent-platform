# 工具目录权限

## 当前状态

工具目录权限统一由 `configs/tools.yml` 中的 `access-policy` 管理。`bash` host shell 与 `file_read`、`file_glob`、`file_grep`、`file_write`、`file_edit` 都读取同一套 `AccessPolicy`，避免 bash 拥有比结构化文件工具更宽的 host path 权限。

`configs/tools.yml` 同时包含 `bash` shell、命令白名单与超时配置，以及 `file-tools` 大小限制、写前读、写审批与 hook 等工具行为配置。旧拆分文件不再读取。

Desktop / 本机服务包会在根目录 `bin/` 内置 `rg`、`dbx`、`httpx`，并仅为 agent-platform 进程及其 Host 子进程前置该目录到 `PATH`。`file_grep`、`file_glob` 使用稳定 JSON schema 并直接调用内置 rg；dbx/httpx 首版通过 JSON `bash` tool 调用，仍受 Bash 命令白名单、bashsec 和 access policy 约束。该 PATH 不会注入 Container Hub sandbox。

## 核心流程

```text
请求 accessLevel
  -> 选择 AccessPolicy level
  -> 展开 @workspace / @chat / @agent / @skills / @skills-center / @owner / @temp
  -> 追加 agent runtimeConfig.hostAccess readRoots/writeRoots
  -> file tools 精确校验目标路径
  -> bash 先跑 bashsec，再校验 cwd、显式 path args、redirect
  -> 越权按 policy allow / hitl / auto / block 执行
```

FileTools 还维护 run-scoped 读写闭环：已有文件写入或编辑前，默认要求同一 run 内先用 `file_read` 完整读取（fully read），防止模型在没有读取最新内容时覆盖用户改动。

不存在 `WorkingDirectory` 或工具级默认目录。相对路径、Bash 未传 `cwd`、以及 `file_glob/file_grep` 省略 path 时一律从 Workspace 解析；`@chat/...` 才访问当前 Chat，`@temp/...` 才访问主临时根。没有 Workspace 时相对路径与 `@workspace` 返回 `workspace_unavailable`，绝不回退 Chat 或进程 cwd。Bash 的单次 `cwd` 可显式使用 `@chat/...` 或 `@temp/...`，并继续服从 AccessPolicy/HITL。

模型可见的工具 schema 会按 session 生成并深拷贝。无 Workspace 时，`bash.cwd` 与 `file_glob/file_grep.path` 会被动态标为必填，文件路径参数会明确要求 `@chat`、`@agent`、`@skills`、`@owner`、`@temp` 等语义根或允许的绝对路径；这只收紧模型契约，不改变 AccessPolicy、路径分类或实际调用时的 `workspace_unavailable` 安全检查。Skill catalog 同时提供精确的 `instructionsPath: @skills/<skillId>/SKILL.md`，命中后应直接用 `file_read` 读取，不应通过 Bash 或目录扫描寻找 Skill。

`@temp` 在进程启动时 canonicalize、去重并冻结；修改 `TMPDIR`、`TMP` 或 `TEMP` 后必须重启。Unix/macOS 同时纳入 `os.TempDir()` 与 canonical `/tmp`，因此 macOS 的 `/tmp`、`/private/tmp` 等价，且 `/var/folders/.../T` 形式的进程 Temp 也有效；Windows 只纳入 `os.TempDir()`，不额外放行 `C:\Windows\Temp`。`@temp/path` 指向主根 `os.TempDir()`，实际 Host 绝对路径可命中任一冻结根。

effective `default.read-roots/write-roots` 即使在旧或自定义 `tools.yml` 中未声明，也会无条件追加 `@temp`。临时根内 FileTools 读、创建、覆盖和编辑不触发路径或 `require-write-approval` 审批；写前读、SHA/mtime/size 并发检查、大小上限、编码和 history 仍执行。Bash 的 `mkdir/touch/cp/mv/rm/chmod` 等可静态识别文件操作不会仅因临时路径触发 HITL，但命令白名单、bashsec、complex/opaque command、`bash-write-in-write-roots` 和各 accessLevel 行为完全不变。管理员 readonly、KBASE mutation gate 与 hard block 仍优先。

临时 Python/JavaScript 脚本推荐先用 `file_write/file_edit` 写入 `@chat/...` 或 `@temp/...`，再发起独立 Bash 调用，并用 `cwd: "@chat"` 或 `cwd: "@temp"` 配合相对脚本路径，例如 `python3 task.py`、`node task.js`。这类解释器仍属于 opaque command：`default` 会要求 access-policy approval；`auto_approve` 直接执行、不生成 `awaiting.ask`，同时写入一次 `decision:auto_approved`、`reason:accessLevel=auto_approve`、`ruleKey:bash-access:opaque:*` 的审计。Host 与 Sandbox 使用相同决策，Sandbox 的 `/chat`、`/tmp` 会映射回 Host 当前 Chat 和冻结临时根。

该流程不建立“本 run 写入即可信”的新权限，也不跳过检查。bashsec hard block、canonical/symlink 逃逸、readonly、KBASE mutation gate 和外部写入规则仍优先；heredoc、输出重定向或“写入并执行”的复合 Shell 命令继续按各自 Bash security 规则判断。

临时权限按最终 canonical 目标判断。词法上位于冻结临时根、但通过 symlink/junction 跳出的路径会直接 block，不能用 HITL 放行；`artifact_publish` 和 `/api/resource` 复用相同解析器。

Workspace 与 Chat 按 canonical 语义分类，不要求普通 Workspace 与 ChatsRoot 物理互斥。分类优先级是当前 Chat、其他 Chat、Workspace（排除整个 ChatsRoot）、external；因此 Workspace 为 `/` 时 Chat 仍不会获得 Workspace 权限或触发 Git/LSP/file-change hook。相对路径或 `@workspace/...` 解析后进入 ChatsRoot 会返回 `path_crosses_chat_root`，当前 Chat 必须改用 `@chat/...`；绝对路径则按最终分类应用 current-chat、other-chat、workspace 或 external 策略。

专用 `mode: KBASE` 无论是否开启 `editingMode` 都固定提供五个文件工具，且 Workspace 始终是最终 `runtimeConfig.workspaceRoot`；当前 Chat 目录是独立的 `ChatDir`。KBASE Workspace、当前 Chat 目录、其他 chatId 和 external 都先经过普通 `AccessPolicy`。run-scoped `ScopedFilePolicy` 不覆盖 AccessPlan，只负责固定工具准入、Workspace canonical 路径识别和 Workspace mutation gate：未开启 editing 时 Workspace 可读但 write/edit 返回 `kbase_editing_mode_required`，不会产生无法生效的 HITL；开启后才应用 Workspace 已有文件强制先读后写和新文件父目录已存在。当前 Chat 目录在两种模式下都按 `@chat` 可读写，external 和其他 chatId 继续按实际 policy 得到 allow、auto、HITL 或 block。`full_access`、hostAccess、writeRoots 和 approval 可以放宽 external，但不能替代 KBASE Workspace 的 `editingMode:true`；管理员显式 block 仍优先。

## 配置与接口

- 工具运行时配置：`configs/tools.yml`，模板为 `configs/tools.example.yml`。
- `access-policy`：目录权限配置。
- `bash`：host shell、命令白名单与超时配置。
- Unix/macOS 默认 host Bash 以 `bash -o pipefail -lc` 执行，管道中任一上游命令非零退出时，整条管道返回最右侧的非零退出码；Windows 与显式配置的非 Bash shell 不注入该选项。
- `bash.shell-args` 是高级完整覆盖：一旦显式配置，平台不再补充默认参数。需要保留默认失败传播语义时，应包含 `-o`、`pipefail`、`-lc` 与 `{{command}}`。
- 默认 Bash 命令白名单包含 `rg`、`dbx`、`httpx`；builtins 版本和来源由发布锁文件管理，不由 tools 配置管理。
- `sandbox-bash`：沙箱 bash 的 bashsec 差异覆盖；只影响 `runtimeConfig.environmentId` agent，不影响 host bash。
- `file-tools`：结构化文件工具行为配置。
- `default`：workspace、chat、temp 写免审；workspace、chat、agent、当前 agent skills、temp 读免审；其他路径触发 HITL。
- `auto_approve`：沿用 `default` 边界；越界读、复杂 bash 与不透明命令自动批准，越界写仍触发 HITL，bashsec hard block 仍然阻断。
- `full_access`：host path 读写全开，仍保留 bashsec hard block。
- `runtimeConfig.hostAccess.readRoots/writeRoots` 可为单个 agent 追加 host-side `bash/file_*` 读写根，支持 `@temp`；该配置不影响 Container Hub / sandbox，sandbox 挂载使用 `runtimeConfig.sandboxMounts`。
- 请求字段 `accessLevel` 可取 `default`、`auto_approve`、`full_access`；缺省为 `default`。
- `/api/access-level` 可在 native run 运行中动态更新当前 run 的 `accessLevel`；该更新不写回 agent 配置，也不会影响下一次 run。
- 动态更新从下一次工具权限判断开始生效，已经开始执行的 host bash / file tool 不会被中断；若当前正等待 access-policy approval，更新后会重新评估，满足新权限时自动继续。
- PROXY / ACP CODER run 暂不支持动态更新，接口返回 `status=unsupported`。
- `auto_approve` 的自动批准会作为 `messages[]` 内联 `role=user` 审计消息写入当前 `chatId.jsonl`，消息内的 `approval.decisions[].decision` 为 `auto_approved`；`full_access` 因为路径根已扩为 `/`，不产生越界审批记录。
- `sandbox-bash.security.bashsec-overrides.output-redirection/heredoc-output-redirection` 可让 sandbox bash 对输出重定向类 bashsec soft approval 自动批准；hard block 仍然阻断。
- `require-write-approval` 控制写入是否默认需要审批。
- `require-read-before-write` 控制写前是否必须先被文件工具完整读取（fully read）。
- `read-before-write-scope` 控制观察范围：`run` 仅限当前 run；`chat` 会在 `<chatId>/.tools/state/file-versions.json` 保存文件版本证明，并允许同 chat 后续 run 在 sha/mtime/size 校验通过时写入。

常见结构化错误：

- `file_unchanged`
- `file_write_not_read`
- `file_modified_since_read`
- `file_edit_not_read`
- `file_edit_modified_since_read`
- `glob_ripgrep_missing`
- `glob_invalid_path`
- `grep_ripgrep_missing`
- `grep_invalid_type`
- `kbase_editing_mode_required`
- `kbase_editing_parent_missing`
- `path_crosses_chat_root`
- `workspace_unavailable`

`file_glob` 用于按文件名/路径 glob 查找文件，`file_grep` 用于按内容正则搜索。两者无命中都不作为错误返回；结果会是 `matchCount: 0`、`results: []`。

## 约束与注意事项

- `access-policy.working-directory`、`bash.working-directory`、`file-tools.working-directory` 已删除，出现即启动失败。旧 YAML 路径策略字段 `bash.allowed-paths`、`bash.path-checked-commands`、`bash.path-check-bypass-commands`、`file-tools.allowed-read-paths`、`file-tools.allowed-write-paths` 同样硬失败。
- 工具权限与服务鉴权不是同一层；JWT、resource ticket、CORS 见 [鉴权与安全边界](鉴权与安全边界.md)。
- `approve_rule_run` 只在当前 run 内批准同一规则，不是永久配置。
- 文件工具适合替代 heredoc、重定向等高风险 shell 写文件方式。
- KBASE Workspace 内的 `file_write` 不会隐式创建目录；开启 editing 后，任何通用文本新文件都只能写入已存在目录，已有 Workspace 文件必须完整读后再写。当前 Chat 目录和获批外部目录沿用普通 FileTools 规则；写入继续执行 SHA/mtime/size 并发校验、原子替换和 run-scoped history。
- `pipefail` 不等于 `set -e`：它只改变单条管道的退出码，不会阻止 `; true`、`|| true` 或后续成功命令覆盖整个 shell 的最终状态。平台默认不启用 `set -e`，普通 Bash 非零退出仍作为可恢复的工具失败交给 Agent 处理。
- `pipefail` 会暴露原先被隐藏的状态，例如 `rg pattern file | wc -l` 在无匹配时返回非零；`yes | head -n 1` 的上游也可能因预期的 SIGPIPE 返回非零。确属预期结果时，应在命令中显式处理。
- sandbox bash 使用同一 AccessPolicy/HITL 评估 cwd 与可静态识别的路径；`/workspace`、`/chat` 会映射回冻结的 Host 双根，sandbox `/tmp` 会映射到冻结的 Host `@temp` 语义根进行判定，实际可见目录仍由 sandbox mounts 控制。
- Container Hub 当前仍以 `/bin/sh -lc` 执行，不注入非 POSIX 的 `pipefail`；只有在 sandbox 镜像统一保证 `/bin/bash` 后才可采用相同默认值。

## 相关文件

- `internal/tools/tool_bash.go`
- `internal/tools/tool_file.go`
- `internal/tools/tool_glob.go`
- `internal/accesspolicy/`
- `internal/filetools/filetools.go`
- `internal/hitl/`
- `configs/tools.example.yml`
