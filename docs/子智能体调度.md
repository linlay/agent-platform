# 子智能体调度

## 当前状态

运行时有三条用途、所有权和生命周期不同的调度路径：

- `agent_invoke`：普通主 Agent 显式声明后调用普通子智能体。
- `agent_delegate`：仅供 orchestrated Team 的隐藏总控委派当前 Team 成员。
- `run_query`（发起独立运行）、`run_status`（查询独立运行状态）、`run_interrupt`（中断独立运行）：普通主 Agent 分别显式声明后发起、查询和中断独立 Agent/Team 根 run。

`agent_invoke` 保持原有契约。`agent_delegate` 定义在 `internal/resources/tools/agent_delegate.yml`，由 Team 自动、强制注入；普通 `agent.yml` 声明该工具会在 catalog 加载时失败，普通 session 与统一工具入口直接执行也会拒绝。隐藏总控和 `agent_delegate` 均不进入普通 Agent 或 Tool catalog。三个 run 工具都是 `explicitOnly` Tool，只有分别在普通 Agent 的 `toolConfig.tools` 中显式挂载后才进入该 Agent 的模型工具集合。旧 `agent_run_query`、`agent_run_status`、`agent_run_interrupt` 已删除，不提供 alias，配置引用旧名会硬失败。

三者不能互换：

| 路径 | run/chat 身份 | 结果回流 | 生命周期 |
|---|---|---|---|
| `agent_invoke` | 复用父 `chatId/runId`，创建 child task/session | 子结果聚合为当前工具结果 | 受父 run 控制 |
| `agent_delegate` | 复用 Team 根 run，成员作为 task | 成员结果回注隐藏总控 | 受 Team 根 run 控制 |
| run 工具组 | 创建或继续目标 chat，并注册独立根 run | query 只返回 run 标识，后续显式 status | 不受调用方 run 中断影响 |

## `agent_invoke`

```text
普通主 Agent 调用 agent_invoke
  -> 主时间线保留 tool 事件
  -> server 并发启动 1~5 个普通子 Agent
  -> 子流实时汇聚到主 SSE / replay
  -> 子结果按输入顺序聚合并回注主 Agent
```

- Agent 必须在 `toolConfig.tools` 显式声明 `agent_invoke`。
- 每项必须包含 `subAgentKey` 和 `task`，可选 `taskName/files`。
- `subAgentKey` 不得等于当前 `Agent Identity.key`；服务端在 catalog 解析前按精确 key 拒绝自调用，含自身目标的整批任务不会启动任何 child。
- 可用主 Agent mode：`REACT`、`CODER`。
- 可用 child mode：`REACT`、`CODER`、`KBASE`、`PROXY`；ACP CODER 当前不能作为 child。
- child session 禁止再次调用子智能体。
- TEAM 隐藏总控不能调用 `agent_invoke`，TEAM 成员也不能携带该工具。
- `Runtime Context: Sub-Agent Candidates` 只提供模型选择其他 Agent 时的候选摘要，不授予调用权限，也不定义 TEAM 的 `agent_delegate` roster。

## 独立根 run 工具组

`run_query`、`run_status`、`run_interrupt` 分别启动、查询和中断独立根 run。它们直接调用平台内部 query admission 与 run-control facade，不通过 localhost HTTP，也不接触 JWT。工具挂载本身就是调用授权：不使用目标白名单，不读取 `visibility` 或 invoke scope；catalog 中存在精确 `agentKey` 或 `teamId` 即可作为目标。

```yaml
toolConfig:
  tools:
    - run_query
    - run_status
    - run_interrupt
```

### `run_query`

```json
{"agentKey":"webOperator","message":"查找最新资料"}
```

```json
{"teamId":"research","message":"研究并复核这个问题"}
```

- `message` 必填，`agentKey/teamId` 必须且只能提供一个。
- `Agent Identity` section 只提供身份事实；`run_query` 的工具描述规定：用户以“当前智能体”“本智能体”“你自己”指定目标时，必须原样使用当前 system prompt 的 `Agent Identity.key`，不得从 `Runtime Context: Sub-Agent Candidates` 推断或替换。
- `chatId` 可选；省略时创建新 chat，提供既有 chat 时目标必须与该 chat 的公开 owner 完全一致，不能把 Agent chat 改绑 Team、替换 Team 或切换 Agent。
- 不接受模型提供 `requestId/runId/role/accessLevel/model/stream`；这些字段由平台掌握。
- 返回前已完成 catalog/admission、Team snapshot、chat/run 注册和 event bus 建立，然后异步执行本地或 Proxy 目标。
- 新 chat 的 source 为 `run-query:<agentKey>`；目标 `request.query` 使用 `runOrigin {agentKey, chatId, runId, toolId}` 记录调用方 Agent、父 Chat、父 Run 与具体工具调用，但不含 subject。
- 同一父 `runId + toolId` 重试返回同一个目标 `chatId/runId`；不同 tool ID 可并行创建独立 run，没有额外并发上限。

### `run_status` 与 `run_interrupt`

```json
{"runId":"..."}
```

```json
{"runId":"...","message":"无需继续"}
```

- status 是只读工具，可在 BTW/read-only 策略中调用；返回 `running/awaiting/completed/failed/interrupted`、`lastSeq` 和时间字段，question awaiting 会返回 `awaiting`，终态返回最终 `content/error`。
- run 工具组不提供 submit/steer；awaiting 必须由用户在目标会话中处理，普通 `/api/submit` 与 `/api/steer` 协议不变。
- interrupt 复用普通 run owner 校验与 Proxy forwarding。
- status/interrupt 只接受由 `run_query` 创建、且调用 Agent 与 subject 均和当前调用方相同的 run；任意外部 run 即使 runId 已知也返回 `run_not_owned`。

### 调用边界

- 只有普通主 Agent 根 run 可以调用；`agent_invoke` child、Team 成员和隐藏协调器均拒绝。
- 允许目标与调用 Agent 自身相同。省略 `chatId` 时创建新 chat；指定另一个 owner 相同且无 active run 的 chat 时允许继续；指定父 run 当前 chat 时沿用单 chat 单 active run 约束并返回 `active_run_conflict`，不会自动替换 `chatId`。
- `run_query` 创建的目标 run 会携带可信 origin 标记并视为 depth 1；即使目标 Agent 配置了任一 run 工具，再次调用仍返回 `run_chaining_not_allowed`。不增加可配置的深度字段。
- `contextConfig.agents` 只影响其他 Agent 的候选提示摘要，当前 Agent 会从摘要中排除；它不是目标 allowlist。不存在的名字分别返回 `agent_not_found` 或 `team_not_found`。
- 缺少主 run 上下文返回 `run_context_required`，child/Team 调用返回 `run_caller_not_allowed`，幂等 query 等待被取消返回 `run_query_cancelled`。

## TEAM 的 `agent_delegate`

目录式 `runtime/teams/<teamId>/team.yml` 创建 orchestrated Team。每个 run 从 embedded definition 克隆 `agent_delegate` schema，并按冻结 roster 动态补充成员枚举和任务上限。

隐藏总控固定拥有四个工具：

```text
agent_delegate
plan_add_tasks
plan_get_tasks
plan_update_task
```

### 参数

`agent_delegate` 只接受 `tasks`：

| 参数 | 必填 | 约束 | 语义 |
|---|---|---|---|
| `tasks` | 是 | 数组，至少 1 项；`maxItems` 等于冻结 roster 大小 | 本次调用需要委派的成员任务 |
| `tasks[].agentKey` | 是 | 必须是动态 `enum` 中的精确成员 key；同一次调用不可重复 | 目标 Team 成员 |
| `tasks[].task` | 否 | string | 自定义任务文本；省略或为空时使用原始用户消息 |
| `tasks[].taskName` | 否 | string | `task.*` 进度事件的标题 |
| `tasks[].files` | 否 | 最多 10 项 | 附加文件或当前 chat 资源，转换成 references 后与原始 references 去重 |

`additionalProperties:false` 同时作用于工具根对象和每个 task。`maxParallel` 不进入工具 schema 的任务上限：它只控制 runtime 同时运行的成员数；`tasks` 数量超过 `maxParallel` 时，仍属于同一次工具调用，由 runtime 分波执行并一次性聚合结果。

目标 `agentKey` 不得等于当前 TEAM session 的执行 Agent key；服务端会在 roster 解析前拒绝自调用，且整批不产生成员任务事件。

成员始终继承原始请求的 `role`、`scene`、`references` 和该成员可见的 Team 历史。自定义 `task` 只替换 message，不会丢失这些上下文。

### 场景枚举

明确由一个成员回答与给单个成员自定义任务是同一条执行路径：

```json
{"tasks":[{"agentKey":"writer"}]}
```

```json
{
  "tasks": [
    {
      "agentKey": "writer",
      "task": "只检查文档中的事实错误",
      "taskName": "事实检查"
    }
  ]
}
```

无法判断合适成员与明确选择多个成员也是同一条多项委派路径：

```json
{
  "tasks": [
    {"agentKey":"writer"},
    {"agentKey":"reviewer"},
    {"agentKey":"researcher"}
  ]
}
```

```json
{
  "tasks": [
    {"agentKey":"researcher","task":"收集事实依据"},
    {"agentKey":"reviewer","task":"检查潜在风险"}
  ]
}
```

一项和多项没有不同的终止规则。每项都产生 `task.start` 和 `task.complete/error/cancel`，全部结果都回注隐藏总控，随后由总控更新计划、继续委派或生成唯一的 Team 最终回答。

工具结果统一为：

```json
{
  "results": [
    {
      "agentKey": "reviewer",
      "taskName": "风险检查",
      "status": "completed",
      "content": "最终成员结果"
    }
  ]
}
```

失败项可增加 `error`、`errorCode`。一个成员失败不会取消同批其他成员；单成员失败和多成员部分失败也走相同的结构化返回路径，由总控决定改派、解释或继续。

## 计划与执行状态

```text
routing -> waiting_members -> coordinator -> complete
```

- 简单请求可以直接调用一次 `agent_delegate`。
- 复杂请求先用 `plan_add_tasks` 创建有序任务列表，再用 `plan_update_task` 维护状态。任务按数组顺序执行且同时最多一个 `in_progress`；只有当前阶段终态后才能启动下一阶段。
- `plan_get_tasks` 用于读取当前计划；计划工具不自动启动成员。
- 当前计划阶段可以在一次 `agent_delegate` 中并行委派多个成员；成员并行不会产生多个活动计划阶段。`agent_delegate` 本身没有 direct、fanout、invoke、DAG 或串行模式。
- 合法状态迁移是 `init -> in_progress -> completed/failed/canceled`；终态不可复活，需要重试时在计划末尾追加新 task。
- 每个用户回合至少成功发起一次 `agent_delegate` 后，总控才能输出最终答案；只调用计划工具不算完成路由。

## 事件、历史与隐私

- 成员事件携带根 `runId`、task `taskId`、`teamId`、成员 `agentKey`、`actor.type:"agent"` 和 `presentation:"task"`。
- 成员正文、reasoning 和 tool chain 不会成为根回答；根 `content`、run summary 与 `AssistantText` 只取隐藏总控的最终 Team 回答。
- 协调器 reasoning、`agent_delegate` 工具卡和原始工具结果不发送给客户端，也不进入普通工具 catalog。
- 成员读取专用 Team 历史：保留自己的 tool chain，只读取其他成员最终正文和 Team 最终正文，不读取其他成员 reasoning/tool result。
- 根总控历史保留用户轮次、Team 最终正文和带成员标识的成员最终正文，排除 child prompt、reasoning、tool chain 与原始中间结果。
- 历史回放仍识别旧 `team_delegate` / `team_invoke` 调用，但只转换为 `agent_delegate agentKeys=...` 安全摘要；新 run 不接受旧工具名。

## HITL 与限制

- 成员不注册独立 active run；public `awaitingId` 使用 `taskId:rawAwaitingId`，submit 时映射回 child raw ID。
- 同一并发波次的多个 awaiting 合并为一个 Team-level form，一次 submit 校验并分发给各 child；超过 `maxParallel` 时后续波次继续合并。
- Team-owned run 的 submit 使用 `teamId + runId + awaitingId`，不能传隐藏总控 key。
- interrupt 作用于整个根 run；steer 写入总控 run，不直接选择某个成员。
- Team 不能作为另一个 Team 的成员或 child 目标。

## 相关文件

- `internal/resources/tools/agent_delegate.yml`
- `internal/resources/tools/agent_invoke.yml`
- `internal/resources/tools/run_query.yml`
- `internal/resources/tools/run_status.yml`
- `internal/resources/tools/run_interrupt.yml`
- `internal/runops/`
- `internal/agent/team/`
- `internal/server/team_coordinator.go`
- `internal/server/frame_orchestrator.go`
- `internal/llm/run_stream_tools.go`
