# 手工测试用例

## 环境变量

```bash
# 本地 make run 与 Docker Compose 默认都走 SERVER_PORT=11949
BASE_URL="http://localhost:11949"
# 若你改了 SERVER_PORT，同步调整 BASE_URL
```

Identity 文件动态注入 Host Bash：

1. 不传 `--identity-file` 启动 Platform，令 `<AP_RUNTIME_DIR>/identity/access-token` 先保持不存在，通过 `test -z "${AP_ACCESS_TOKEN+x}"` 确认普通 Agent Bash 中没有该变量。
2. 不重启 Platform，写入合法单行 token，通过只检查存在性或预期哈希确认下一次 Bash 获得新值；原子替换文件后下一次 Bash 使用替换值，删除文件后下一次 Bash 不再包含该变量。测试输出不得打印 token 原文。
3. 另建 token 文件并使用 `--identity-file <absolute-path>` 启动，确认显式文件覆盖 `<AP_RUNTIME_DIR>/identity/access-token`。
4. 设置父进程或 Agent runtime env 的同名变量，确认不能绕过有效 identity 文件；Agent/Skill/调用级声明应以保留变量错误拒绝。
5. 确认 Workspace Terminal、`file_grep/file_glob`、Container、Proxy、ACP、MCP、LSP 与 sidecar 未自动获得 `AP_ACCESS_TOKEN`，已有 Bash 不因文件变化被中断。

## Catalog 接口

```bash
curl -X GET "$BASE_URL/api/agents"
```

```bash
curl -X GET "$BASE_URL/api/agents?includeChats=5"
```

```bash
curl -X GET "$BASE_URL/api/agents?includeTeam=true&includeChats=5"
```

启用 `includeTeam` 后，每项应带 `kind:"agent" | "team"`；Team 以 `teamId` 标识，带 `stats` 和可选 `chats`，但不应出现空的 `key` 或 `mode`。混合列表按最新 chat 的 `lastRunId` 从新到旧排列；`mode` 与 `scope` 仅影响 Agent，不排除 Team。

```bash
curl -X GET "$BASE_URL/api/agents?scope=nav&mode=REACT,CODER"
```

```bash
curl -X GET "$BASE_URL/api/agent?agentKey=default_agent"
```

```bash
curl -X GET "$BASE_URL/api/skills?agentKey=default_agent"
```

响应 `data` 应只包含 `agentKey` 和 `skills`；每个 Skill 包含 `key/name/agentHasSkill` 与可选 `description`，不得出现 `items` 或 `meta`。Agent 已配置 Skill（包括 Agent 私有 Skill）应在前并标记为 `true`，其余有效技能中心 Skill 标记为 `false`。省略 `agentKey` 应返回 400 `agent_key_required`，不存在的 Agent 应返回 404 `agent_not_found`。WebSocket route `/api/skills` 使用 payload `{"agentKey":"default_agent"}`，成功 response 的 `data` 应与 HTTP 相同。

```bash
curl -X GET "$BASE_URL/api/teams"
```

响应中的每个 Team 不得包含 `runtimeMode` 或 legacy runtime metadata。Team 的 `meta` 应包含 `orchestrated:true` 与 `maxParallel`，但不得出现隐藏总控 key、模型配置、prompt 或 `agent_delegate`。`GET /api/admin/tools` 同样不得列出 `agent_delegate`。

```bash
curl -X GET "$BASE_URL/api/admin/skills"
```

导入一个根目录包含 `SKILL.md` 的完整 ZIP；也应接受只有一层包装目录的 ZIP：

```bash
curl -sS -X POST "$BASE_URL/api/admin/skills/import" \
  -F 'key=imported-skill' \
  -F 'file=@./imported-skill.zip;type=application/zip'
```

成功响应应返回 `data.skill.key="imported-skill"` 和 `data.openedFile.path="SKILL.md"`。再次使用相同 Key 应返回 409；缺少 `SKILL.md` 或包含 `../`、symlink、无效 `.runtime-env.json` 的 ZIP 应返回 422 和 `data.error.diagnostics[]`，且 `skills-center/imported-skill` 不得留下半成品。非 ZIP 返回 415，超过压缩、单文件、总解压量或 4096 entry 限制返回 413。

### Agent 专属 Skill 导入

在 `/agents` 编辑一个已保存的目录型 Agent，在“技能”区域点击“导入专属技能”并上传包含 `SKILL.md` 的 ZIP。系统应从 frontmatter 的 `key`（没有则 `name`）自动识别新 Key。成功后：该 Key 自动选中，`GET /api/admin/agents/detail?agentKey=<key>` 的 `privateSkills[]` 包含它，磁盘只出现 `agents/<agentKey>/skills/<skillKey>/`，而 `/api/admin/skills` 不应出现该 Key。下拉标签按“名称 · 专属”显示；专属管理行按“[专属] 名称”显示，右侧“删除”会二次确认并删除本地目录及 YAML 引用。下拉标签的移除仅解除启用，不删除本地目录。

使用已存在于技能中心的 Key 时，导入应直接成功，不查询或要求确认技能中心版本；仅当前 Agent 显示并使用“名称 · 专属”，不追加覆盖说明。此时删除该技能中心 Skill 不应被该 Agent 阻塞，且不会删除其本地覆盖版本。

```bash
curl -X GET "$BASE_URL/api/admin/tools"
```

## MCP 2025-11-25

查看 registry summary。HTTP 项应有 `transport:"streamable-http"` 和 `baseUrl`；stdio 项应有 `transport:"stdio"`，且不得出现 `command`、`args`、`env` 或无意义的 `baseUrl`：

```bash
curl -sS "$BASE_URL/api/admin/registries"
```

Qiuerscript 迁移后，六个工具应由 MCP catalog 发现，来源固定为 `sourceType/sourceCategory:"mcp"` 且 `serverKey:"qiuerscript"`：

```bash
curl -sS "$BASE_URL/api/admin/tools"
```

在已配置 qiuerscript `dev` datasource 的机器上，可直接验证标准 MCP stdio 初始化、`initialized`、`tools/list` 与 stdin EOF 退出：

```bash
printf '%s\n' \
  '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"manual-test","version":"1"}}}' \
  '{"jsonrpc":"2.0","method":"notifications/initialized","params":{}}' \
  '{"jsonrpc":"2.0","id":2,"method":"tools/list","params":{}}' \
  | qiuerscript-tool serve --datasource dev
```

`tools/list` 应返回 `qs_read/qs_glob/qs_grep/qs_write/qs_edit/qs_delete`；管道结束后进程应正常退出。把 initialize 中的版本改为 `2025-06-18` 时，server 应拒绝并在 stderr 明确提示要求 `2025-11-25`。

旧配置硬失败回归：在隔离 runtime 的 `tools-dir` 分别放入 `service.yml`、`type: external`、`external: {}` 或 `kind: external-service` 后启动/触发 tools 热重载，操作应失败并提示迁移到 `registries/mcp-servers` 的 `transport: stdio`；不要在共享运行目录执行此用例。

## 会话接口

```bash
curl -X GET "$BASE_URL/api/chats"
```

```bash
curl -X GET "$BASE_URL/api/chats?lastRunId=test-run-id"
```

```bash
curl -X GET "$BASE_URL/api/chats?agentKey=default_agent"
```

```bash
curl -X GET "$BASE_URL/api/chats?mode=unknown"
```

全局 `/api/chats` 的 Team-owned chat 不受 `mode` 过滤；显式 `agentKey` 时仍只返回该 Agent 的 chat。

```bash
curl -X POST "$BASE_URL/api/read" \
  -H "Content-Type: application/json" \
  -d '{"chatId":"replace-me"}'
```

```bash
curl -X GET "$BASE_URL/api/chat?chatId=replace-me"
```

```bash
curl -X GET "$BASE_URL/api/chat?chatId=replace-me&includeRawMessages=true"
```

从已完成 run 派生新 chat，随后用新 `chatId` 继续 query：

```bash
curl -sS -X POST "$BASE_URL/api/chat/derive" \
  -H "Content-Type: application/json" \
  -d '{"sourceChatId":"replace-me","sourceRunId":"replace-run-id","chatName":"派生会话"}'
```

```bash
curl -N -X POST "$BASE_URL/api/query" \
  -H "Content-Type: application/json" \
  -d '{"chatId":"derived-chat-id-from-response","message":"基于派生上下文继续回答","agentKey":"default_agent"}'
```

```bash
curl -X POST "$BASE_URL/api/chats/search" \
  -H "Content-Type: application/json" \
  -d '{"query":"replace-me","agentKey":"default_agent","limit":10}'
```

```bash
curl -X GET "$BASE_URL/api/chat/export?chatId=replace-me"
```

```bash
curl -X GET "$BASE_URL/api/archives?agentKey=default_agent"
```

```bash
curl -X GET "$BASE_URL/api/archive?chatId=replace-me"
```

```bash
curl -X POST "$BASE_URL/api/archives/search" \
  -H "Content-Type: application/json" \
  -d '{"query":"replace-me","agentKey":"default_agent","limit":10}'
```

## Query 回归测试

HTTP JSON 模式：

```bash
curl -sS -X POST "$BASE_URL/api/query" \
  -H "Content-Type: application/json" \
  -d '{"message":"用一句话介绍 agent-platform","agentKey":"zenmi","stream":false}'
```

```bash
curl -sS -X POST "$BASE_URL/api/query" \
  -H "Content-Type: application/json" \
  -d '{"message":"用一句话介绍 agent-platform","agentKey":"zenmi","stream":false,"includeUsage":true,"includeFullText":true}'
```

SSE 流式模式：

```bash
curl -N -X POST "$BASE_URL/api/query" \
  -H "Content-Type: application/json" \
  -d '{"message":"元素碳的简介，100字","agentKey":"default_agent"}'
```

```bash
curl -N -X POST "$BASE_URL/api/query" \
  -H "Content-Type: application/json" \
  -d '{"message":"列出当前仓库的 Go 文件数量，并说明你会调用什么工具","agentKey":"default_agent"}'
```

```bash
curl -N -X POST "$BASE_URL/api/query" \
  -H "Content-Type: application/json" \
  -d '{"chatId":"replace-me","message":"继续上一轮内容","agentKey":"default_agent"}'
```

```bash
curl -N -X POST "$BASE_URL/api/query" \
  -H "Content-Type: application/json" \
  -d '{"agentKey":"default_agent","runId":"replace-me","message":"使用指定 runId 发起一次请求"}'
```

### Model turn 提交、丢弃与恢复

使用可控制 stream 帧的 mock provider，并把测试 Agent 的 `budget.model.retryCount` 设为至少 `1`。每个场景同时检查 live SSE、`GET /api/chat`、run summary 和磁盘 `<chatId>.jsonl`：

1. 正常结束但不返回 usage/context window：应正常 commit 并只写一条 `react`。
2. 先返回 usage，再返回缺 id、非法类型或缺函数名的 tool call：不得 commit；usage 不能使该 turn 落盘。
3. 分别在 content、reasoning 和 tool arguments 中途断流：第一次 attempt 应收到 `run.activity.status:"retrying"` 与 `recovery.action:"discard_incomplete_model_turn"`；recovery 中对应 id 必须覆盖已展示的半截块。
4. 重试成功：JSONL 和 run summary 只包含成功 attempt，`react.seq` 连续且没有空洞；非流式 `content/fullText` 也不得拼入第一次 attempt。
5. 重试耗尽：只出现终止 `run.error`，JSONL 中没有未完成 `react`。
6. 完整 tool call commit 后让工具返回失败：原 assistant turn 与同 `seq` 的失败 tool result 都应保留，不出现 model turn recovery。
7. 工具执行期间中断或制造可能有副作用的失败：不得自动回滚重试该 model turn，避免工具重复执行。
8. 对 OpenAI、Anthropic、HITL awaiting、planning、orchestrated Team 和并行工具分别重复正常/断流用例；成员 task 的 recovery id 与根 task 不得串 scope。

旧历史回归准备两个失败 run fixture：

- 末尾 `file_write` arguments 截断且无 tool result：`/api/chat`、raw messages、search、compact、derive 和后续 LLM history 应忽略整个末尾 group；原始 JSONL 审计内容仍保留该文本。
- 末尾 tool call 结构完整但无结果：继续 query 应在本地返回 HTTP `409` 与 `data.error.code:"chat_history_incomplete"`，并确认 mock provider 没有收到请求。

### 工具预算耗尽终态

把测试 Agent 的 `budget.tool.maxCalls` 设为 `1`（等价生产回归可设为 `60`），让 mock provider 连续返回两次合法工具调用。第二次对应第一个越限尝试。分别用默认 SSE 和 `stream:false` 执行，并检查：

1. ToolRouter 只收到第一次调用；第二次不执行，且不存在第三次模型请求。
2. SSE 顺序为一次失败 `tool.result(error:"tool_calls_exceeded")`、一次 `run.error(error.code:"tool_calls_exceeded")`、`[DONE]`；之后没有 `run.complete` 或 `run.cancel`。
3. `run.error.error.diagnostics` 包含 `toolCalls:2`、`limitValue:1`、`limitName:"budget.tool.maxCalls"` 和实际 `toolName`。
4. 并行批次中预算内 sibling 正常收尾，所有越限 sibling 均不执行；客户端只收到一次越限 `tool.result` 和一次终止 `run.error`。
5. `GET /api/attach` 和 WebSocket run stream 可回放同一完整错误；`run_status` 返回 `status:"failed"` 与相同 `error.code`。
6. run summary 的 `finishReason` 为 `error`；`<chatId>.jsonl` 含 `_type:"event"` 的完整 `run.error`；刷新 `GET /api/chat` 后只出现一个 `run.error`，不出现 `run.complete/run.cancel`。
7. 在服务端确认预算错误后调用 `/api/interrupt`，响应必须为 `accepted:false, status:"unmatched"`，数据库、`run_status` 和回放仍保持 error。
8. `stream:false` 返回错误 envelope，但其数据库、RunControl、事件总线和 Chat 回放结果与 SSE 路径一致。
9. 让同一模型响应返回至少三个不可并行的 `plan_update_task`，并使预算在批次中途耗尽：客户端仍只收到一次越限 `tool.result` 和一次 `run.error`；JSONL 中每个 `tool_call_id` 都有结果，首个越限调用之后的 sibling 结果带 `_internalOnly:true`，其内容含 `executed:false`。
10. 在上述失败 run 后对同一 Chat 发起新 query，确认历史加载成功、请求到达 provider 且不返回 `409 chat_history_incomplete`；人工构造的未知执行状态缺失结果历史仍必须返回 409。

## 时间契约回归

完成一次 query 后，确认 chat detail 直接提供顶层时间，且所有结构化时间都是 JSON number（不是字符串、`0` 或 `null`）：

```bash
curl -sS "$BASE_URL/api/chat?chatId=replace-me" | jq '
  .data |
  {createdAt, updatedAt, runTimes: [.runs[]? | {startedAt, completedAt}], eventTimes: [.events[]? | .timestamp]}
'
```

`createdAt`、`updatedAt`、每个 run 的 `startedAt` / `completedAt` 与每个 event 的 `timestamp` 必须位于 `1000000000000..9007199254740991`。可选字段（例如 `readAt`、`expiresAt`、KBASE `lastIndexedAt`）未发生时必须不存在，不能以 `0` 或 `null` 表示。

对 proxy / child SSE、WebSocket adapter 注入任意一项负例：`"timestamp":"1700000000000"`、`"timestamp":1700000000`、`"timestamp":1700000000000.5` 或缺少 `timestamp`。未写出响应的 HTTP 流必须返回 `422` 和 `data.error.code:"time_contract_violation"`；已开始的流必须只追加平台本地、带合法 `timestamp` 的 `run.error` 后结束。

使用旧 JSONL、archive 或 trace 夹具时，故意保留字符串、秒级、浮点、`0` 或缺失的必填时间，确认 `/api/chat`、`/api/chat/jsonl`、archive replay、search 与 `/api/chat/llm-trace` 返回 `422 time_contract_violation`，且文件没有被重写。

## KBASE LanceDB 回归测试

以下示例假设 `docs_kbase` 是 `mode: KBASE` 的 required Agent，`zenmi` 是目录式 `mode: REACT` Agent 且配置了 `kbaseConfig.enabled: true`；两者都通过 `runtimeConfig.workspaceRoot` 指向各自的 knowledge Workspace。两个 Workspace 中至少各有一份中文和一份英文文档，embedding provider 与 Lance sidecar 可用。`configs/kbase-settings.yml` 不得包含 `storage` 或 `migration` 段。

先确认 `zenmi` 保留自己显式配置的普通工具，同时自动获得 `kbase_search`、`kbase_files`、`kbase_read`、`kbase_status`、`kbase_refresh`；配置中不应要求重复声明这五个工具。将相同配置依次改成 `PLAN-EXECUTE` 和原生非 ACP `CODER` 也应加载成功；ACP CODER、`PROXY`、`CHANNEL` 应在 catalog 校验阶段被拒绝。普通 Agent 缺少显式 `enabled: true`、enabled KBASE 缺少 Workspace、旧 `kbaseConfig.source`、Workspace 为 `@chat`、文件系统根或与 ChatsRoot 重叠时均应失败。`enabled: false` 应保留普通 Agent，但不出现 KBASE 工具、capability prompt 或 watcher。

检查初始状态：

```bash
curl -sS "$BASE_URL/api/kbase/docs_kbase/status"
```

首次索引前应为 `data.engine=lancedb` 且 `stale=true`；Lance 就绪时应检查：

- `workspaceRoot` 是唯一的 knowledge Workspace；响应不再包含 `sourceRoot`。
- `schemaVersion="4"`、`generation.state="active"`、`sidecar.available=true`、`sidecar.protocolVersion=2`。
- `indexes.fts.ready=true`、`indexes.vector.ready=true`。
- `pendingRecoveryOperations=0`。
- `files/chunks` 与当前 workspace 的抽取结果一致。

执行普通 refresh：

```bash
curl -sS -X POST "$BASE_URL/api/kbase/docs_kbase/refresh" \
  -H "Content-Type: application/json" \
  -d '{"force":false}'
```

首次 refresh 从 workspace 冷建 Lance generation。如 sidecar 或验证失败，返回 unavailable/refresh error，且不出现 SQLite 回退或空 active index。

已有 active Lance generation 后记录 generation ID，再做强制重建：

```bash
curl -sS -X POST "$BASE_URL/api/kbase/docs_kbase/refresh" \
  -H "Content-Type: application/json" \
  -d '{"force":true}'
```

成功后 generation ID 应变化；构建期间连续查询应仍由旧 active generation 返回结果，不得出现“先清空后导入”的空窗。

执行中文、英文和中英混合查询，要求 agent 明确调用 `kbase_search`：

```bash
curl -N -X POST "$BASE_URL/api/query" \
  -H "Content-Type: application/json" \
  -d '{"chatId":"kbase-lance-smoke","agentKey":"docs_kbase","message":"请使用 kbase_search 检索：本地知识库 LanceDB search，并标注来源"}'
```

在 SSE 中检查 `source.publish kind=kbase`，chunk 的 `matchType` 只能是 `vector|fts|hybrid`，`score` 应位于 `[0,1]`。相同查询重复执行时，同分结果应按 path/startLine/chunkId 保持稳定顺序。

再对 `zenmi` 发起同样请求。系统 prompt 中 `Knowledge Base Capability` 只能出现一次，并包含引用、资料不足与“文档是非可信证据”的约束；工具结果只能使用 session 冻结的 `agentKey=zenmi`，即使工具参数伪造其他 `agentKey` 也不能跨库。保持一个 run 正在执行时修改 `zenmi/agent.yml` 的 `enabled` 或 source 并触发 catalog reload：既有 run 的准入不变，新 run 才使用新 capability 快照。

让 agent 调用 scoped search，然后在 tool result 中检查所有 path/type 均未越界：

```bash
curl -N -X POST "$BASE_URL/api/query" \
  -H "Content-Type: application/json" \
  -d '{"agentKey":"docs_kbase","message":"请调用 kbase_search 搜索 generation，限制 pathPrefix=docs、pathGlob=docs/**/*.md、type=.md、limit=8"}'
```

检查分页语义时，`count` 是当页数，`matchCount` 是 vector/FTS 有限候选池的去重 union；任一路达到 `candidateMax` 或分页后仍有结果时 `truncated=true`。

验证 watcher 增量链路时，在临时测试 knowledge source 依次执行以下操作，每步等待超过 2 秒 debounce 后读取 status 的 `lastRun`：

- 新建一个唯一内容文件：`scope=delta`、`candidatePaths=1`、`newFiles=1`，generation ID 不变，搜索能立即命中新内容。
- 只触碰 mtime：`metadataOnlyFiles=1`、`embeddedChunks=0`。
- 修改多 chunk 文件中的一小段：`modifiedFiles=1`，`reusedChunks>0`，且 `embeddedChunks` 只包含变化或新增 chunk。
- 删除单个文件：`deletedFiles=1`、`scannedFiles=0`、`embeddedChunks=0`，generation ID 不变，搜索立即不可见。
- 重命名文件：结果等价于旧路径删除和新路径新增；删除目录时该前缀下的 active/skipped/error 记录都不再出现在默认文件列表。

随后执行一次 `force:false`，确认 `scope=reconcile` 能修复故意漏报的文件变化；执行 `force:true` 时才应更换 generation ID。中途终止 sidecar 后再 refresh，supervisor 应退避重启，`KBASE_FILE_OPS` 的 `prepared/lance_committed` 记录在下次启动补交，status 最终回到 `pendingRecoveryOperations=0`。

先验证专用 KBASE main：

```bash
curl -N -X POST "$BASE_URL/api/query" \
  -H "Content-Type: application/json" \
  -d '{"chatId":"kbase-main-smoke","agentKey":"docs_kbase","message":"读取 Workspace 中的 docs/policy.md；再把普通文本报告写到系统提示提供的明确 chat_dir 路径。不要修改 Workspace"}'
```

确认 system-init cache 为 `kbase:main`，Workspace 为最终 `runtimeConfig.workspaceRoot`，`ChatDir` 是独立的当前 Chat 目录；相对路径落在 Workspace，`@chat` 路径落在 Chat。工具集包含五个文件工具但没有 Bash。Workspace write/edit 返回 `kbase_editing_mode_required` 且不出现 awaiting approval。

再验证专用 KBASE editing：

```bash
curl -N -X POST "$BASE_URL/api/query" \
  -H "Content-Type: application/json" \
  -d '{"chatId":"kbase-editing-smoke","agentKey":"docs_kbase","editingMode":true,"message":"先读取 docs/policy.md，把退款期限改为 14 天，并报告 lineStats；说明索引由 watcher 异步维护"}'
```

确认 `request.query.editingMode=true`，system-init cache 为 `kbase:editing`，Workspace 与 main 一样仍为 `runtimeConfig.workspaceRoot`，两种 stage 的工具 schema 完全相同且没有 Bash；Workspace mutation 成功且结果不包含 `kbase-index` hook。等待超过 watcher debounce 后，status 的 delta run 和搜索结果再反映新内容。继续覆盖：

- 非 editing Workspace gate：相对路径、绝对路径、已有文件、新文件、`../` 和从 Chat 软链接进入 Workspace 的 write/edit 均返回 `kbase_editing_mode_required`；`full_access`、auto approve、writeRoots、`runtimeConfig.hostAccess.writeRoots` 和既有 approval 均不能放宽，也不生成无意义的 HITL。管理员 Block 仍优先返回 path-blocked。
- Workspace：开启 editing 后，在现有目录内新建和搜索 `.md/.txt/.json/.html` 与自定义文本扩展名；使用通用支持的非 UTF-8 编码读写并确认编码保留；缺失父目录仍拒绝。
- Workspace 已有文件：未完整 `file_read` 时 write/edit 返回写前读错误；读后成功，并继续检查 SHA/mtime/size 并发保护与 history。
- 当前 Chat 目录：两种模式下 read/glob/grep/write/edit 普通文本都成功，mutation 不触发 KBASE 索引。
- external：default 下 read/write/edit 进入各自的 AccessPolicy HITL，批准匹配 canonical target 后成功；read approval 不能复用为 write approval。
- 策略放宽：配置 `writeRoots`、`runtimeConfig.hostAccess.writeRoots` 或 `full_access` 后 external write/edit 直接成功；管理员 `block` 时仍拒绝且不产生 awaiting approval。
- canonical 路径：`../`、绝对路径和 Workspace/Chat symlink 都按实际目标重新应用 AccessPolicy 与 Workspace gate，伪造路径分类或重放其他 approval 无效。
- chat 隔离：其他 chatId 按 other-chat 独立分类，可进入 HITL 或被显式策略授权，不享受当前 `@chat` 或 Workspace 权限。

把 Workspace 目标路径加入 exclude 或使用 extractor 不支持的文本扩展名后，文件仍应保存；等待 debounce 后 watcher 跳过该文件。中止 sidecar 后写入仍成功，watcher 失败使 status degraded；恢复 sidecar 后等待 reconcile 或在用户明确要求下调用一次 `kbase_refresh`。对 `zenmi`、CODER、PROXY 和 Team 传顶层 `editingMode:true` 必须返回 `400 editing_mode_unsupported`，`params.editingMode` 不得生效。

最后分别配置 KBASE Workspace 为文件系统根、与 ChatsRoot 相等、互为父子和 symlink 实际重叠，重启或热重载 catalog。仅目标 Agent 应从运行时 catalog 隔离，管理端诊断码为 `invalid_kbase_workspace_overlap`，其他 Agent 正常；依赖 Team 显示该成员不可用。修正 Workspace 后再次热重载，Agent 应恢复。

Release runner 应在真实 sidecar 环境执行首次索引、增量 refresh、强制 generation rebuild、generation rollback、文件操作恢复和 sidecar unavailable 的 smoke；其中任何失败都不得回退或读取遗留 SQLite 检索库。

验证 chat replay：

```bash
curl -sS "$BASE_URL/api/chat?chatId=kbase-lance-smoke"
```

应从 `react-tool` 的 `sources.items[]` sidecar 恢复 `source.publish`，并保留 `liveSeq`。

### Artifact manifest 与审计 sidecar

让 agent 直接生成一张图片，确认 `image_generate.images[0]` 同时包含内部绝对 `path` 与不带前导 `/`、不含 chatId 的 `<filename>` 逻辑 `url`，文件位于 Chat 根目录；最终 Markdown 只能出现该 `url`，不得出现 `path`、`file://`、`chat_dir`、`@chat` 或主机绝对路径。再把该 `path` 传给 `artifact_publish`，确认发布文件位于 `<chatId>/artifacts/<runId>/<filename>`，`publishedArtifacts[0].url` 指向发布副本且最终回答改用该 URL，不再引用生成源 URL。

分别选择 GPT Image 2、Gemini 3.1 Flash Image、Gemini 3.1 Flash Lite Image 三个 BabelArk profile，各做一次文生图和一次单图编辑。输入图统一写成 `{"source_type":"reference_name","value":"target.png"}`。GPT Image 2 文生图必须访问 `/v1/images/generations`，编辑必须访问 `/v1/images/edits` 并发送 `image[]` multipart；两个 Gemini profile 的文生图和编辑都必须只访问 `/v1/chat/completions`，编辑包含 `image_url` content block，绝不访问 Images endpoints。成功结果分别为 `operation: generation`、`operation: edit` 并物化到当前 Chat。

对 GPT Image 2 追加与第一张图同尺寸的新格式 mask，依次验证 `alpha`、`white_edit`、`black_edit`，结果应为 `operation: inpainting`。尺寸不一致、alpha 模式使用非透明 PNG、mask 不带 images 都必须在 provider 调用前失败。对两个 Gemini profile 传 mask，确认返回 `image_generate_mask_unsupported`，且不会改用 GPT Image 2 或发送提示词近似请求。再以 `{"source_type":"file_path","value":"<越权路径>"}` 验证会生成 AccessPolicy HITL，拒绝后不得读取文件；HTTP URL source type、空 value、字符串元素以及旧的 `reference_name/file_path` 属性必须在 provider 调用前拒绝，并返回图片索引和新格式示例。

在 SSE 中确认成功发布必有 `artifact.publish`，其顶层 `timestamp` 是 epoch milliseconds，`toolId` 与触发发布的 `tool.result.toolId` 相同；每个 artifact 都有 `artifactId/name/mimeType/sizeBytes/sha256/url`，Team 子任务还应带正确的 `taskId`。检查 `<chatId>/.tools/artifacts.json`：其中的 `chatId`、artifact 元数据、`runId` 与 `publishedAt` 都存在。

检查 JSONL：只有包含该 `tool.result` 的 `_type:"react-tool"` 行携带 `artifacts.items[]`，且项目仅描述本次批量发布；后续 `react-tool` 行不得复制累计 artifact 快照。再调用 `/api/chat`，确认 `data.artifact.items[]` 来自 manifest。将 chat 归档/恢复并派生到已完成 run，确认归档恢复仍能返回 artifact，而派生 chat 的 manifest 只保留该 run 的项目且 resource URL 指向新 chat。

验证缺 sidecar 时，只在隔离的 release 副本中移除 `bin/kbase-lance-engine[.exe]`；不要操作生产安装目录。存在 `docs_kbase` 这类 required capability 时，KBASE 应返回 unavailable 且 `/healthz` 返回 `503`。只保留 `zenmi` 这类 optional capability 后重启，`/healthz` 应返回 `200` 并携带 `degraded/error`，Zenmi 的普通工具仍可用，而五个 KBASE 工具返回 `kbase_unavailable` 与 `stale=true`。两种情况都不能读取或回退 SQLite 检索库。

SQLite 认领回归（仅在隔离 runtime 执行）：先用当前版本创建 `chats.db`、`archive.db`、`memory.db` 或 KBASE `control.db`，将两个 PRAGMA 都设为 `0`。重启时，结构完全相同的库应被补写当前 `application_id` / `user_version`；将任一库加入额外列、表、索引、触发器或 FTS 对象后，标记必须仍为 `0/0` 且数据库不被修复。chat/archive/memory 的不匹配应阻止启动；required KBASE 不匹配应把对应 Agent 显示为 `invalid_kbase_storage`，引用该成员的 Team 不可运行；optional KBASE 不匹配应保留普通 Agent，status 为 degraded/stale，修复目录并成功 refresh 后降级状态消失。进程启动完成后再把一份匹配库改回 `0/0` 并触发热重载、KBASE status/search/refresh 或只读打开，标记不得被重新写入。

本地 build cache 由相邻项目主动构建：

```bash
./scripts/sync-local-builtins.sh
```

在 Darwin ARM64，确认 cache 的 `bin/` 包含 `rg`、`dbx`、`httpx`、`kbase-lance-engine` 与 `pdftotext`，且存在 `libexec/poppler-pdftotext/darwin-arm64/`。本地 clean checkout 的 `VERSION` 严格高于组件目标时必须询问；输入 `yes` 后只允许更新组件目标与 `darwin-arm64` 的 target release/path/SHA，并先在相邻项目稳定 `dist/<version>/` 固化同 SHA archive，所有 Windows/Linux target 必须逐字节不变。输入其他内容或非交互 leader 均不得修改正式 lock。随后在 Windows AMD64 checkout 到目标 VERSION/commit 并同步，应不询问而自动只更新 `windows-amd64` target；checkout/commit 不匹配则只更新 cache。rollout 未完成时再发布更高版本，应允许新 leader 经 `yes` 抢占，落后平台随后直接跟随最新目标。同版本不同 commit/path/SHA、dirty、降级、artifact 目标冲突、校验失败或 lock 并发变化均不得覆盖正式记录。`--all` 不得为未在 lock 中声明的 Poppler target 尝试构建，也不得写交叉 target。故意令任一构建 archive、Cargo metadata、SBOM、Poppler runtime manifest 或 payload 缺失时必须失败，且不得替换既有 `build/builtins/<target>`。同步脚本不操作 `release-local/bin`。六平台 build/start/search smoke、签名与 notarization 在外部 sidecar 项目的 release CI 执行，不得因为本地同步脚本存在就记录为已验证。

正式 Desktop builtin 发布不需要 platform 仓库保存 `dist/kbase-lance-engine`。在 Darwin ARM64 release runner 上，先确保相邻 builtin collection 中的 lock target archive 已发布，再从干净 platform 目录验证装配：

```bash
make test-release-program-clean
```

该命令会删除本仓库 `dist/`，再直接运行 `make release-program`；不要求 Rust、Cargo 或 `protoc`，但要求锁定 builtin archive 及其 Cargo metadata/SBOM 均已就绪。它会检查最终 bundle 的 sidecar、metadata 和 `verify-program-bundle`；Desktop 仅调用 Platform 的 `make release`，不会传递 sidecar 配置。

## Team 调度回归测试

以下示例假设已有目录式 Team `research`，至少包含 `writer`、`reviewer` 两个可作为 child 的成员。平铺 Team 文件必须使启动失败；Team 请求一律只传 `teamId`。

Team 只传 `teamId`。明确指定一个成员时，应看到该成员的 `task.start/complete` 和 `presentation:"task"` 内容，随后由总控输出唯一根回答；成员正文不能直接成为根回答：

```bash
curl -N -X POST "$BASE_URL/api/query" \
  -H "Content-Type: application/json" \
  -d '{"chatId":"team-single-chat","teamId":"research","message":"请让 writer 起草一段项目简介"}'
```

意图不明确或明确需要多个成员时，应走相同的多项委派：每个成员出现带 `taskId/agentKey/teamId/presentation:\"task\"` 的 content/task 事件，最后只有一个 Team 根回答；stream 中不应出现 `agent_delegate` 工具卡或总控 reasoning：

```bash
curl -N -X POST "$BASE_URL/api/query" \
  -H "Content-Type: application/json" \
  -d '{"chatId":"team-multi-chat","teamId":"research","message":"请 writer 和 reviewer 从各自视角评估这个方案"}'
```

复杂请求用于观察 plan tools 驱动的多轮 `agent_delegate`：计划工具本身不执行成员，至少成功委派一次后总控才能结束：

```bash
curl -N -X POST "$BASE_URL/api/query" \
  -H "Content-Type: application/json" \
  -d '{"chatId":"team-plan-chat","teamId":"research","message":"先由 writer 起草，再由 reviewer 复核，根据复核意见输出最终版本"}'
```

尝试用 `agentKey` 绕过 Team 应返回 400：

```bash
curl -i -X POST "$BASE_URL/api/query" \
  -H "Content-Type: application/json" \
  -d '{"teamId":"research","agentKey":"writer","message":"绕过协调器"}'
```

检查 Team chat 公共 owner；`teamId` 应为 `research`，根 `agentKey` 为空或省略：

```bash
curl -sS -X GET "$BASE_URL/api/chat?chatId=team-multi-chat"
```

## Submit / Steer / Interrupt

`/api/submit` 当前使用统一 HITL 协议：

- 顶层固定是公开 owner + `runId + awaitingId + params`；普通 Agent 使用 `agentKey`，Team 使用 `teamId`
- `params` 永远是数组
- 不在 submit 里再传 `mode`
- 后端按 `awaitingId` 反查当前是 `question / approval / form`
- `params[i]` 固定对应 `awaiting.ask` 下发数组的第 `i` 项；`id` 可省略，即使携带也只作审计用途

question:

```bash
curl -X POST "$BASE_URL/api/submit" \
  -H "Content-Type: application/json" \
  -d '{"agentKey":"default_agent","runId":"replace-me","awaitingId":"tool_question","params":[{"id":"q1","answer":"Weekend"},{"id":"q2","answers":["产品更新","使用教程"]}]}'
```

也可以省略 `id`，只要顺序与 `awaiting.ask.questions` 一致：

```bash
curl -X POST "$BASE_URL/api/submit" \
  -H "Content-Type: application/json" \
  -d '{"agentKey":"default_agent","runId":"replace-me","awaitingId":"tool_question","params":[{"answer":"Weekend"},{"answers":["产品更新","使用教程"]}]}'
```

question 的每个答案必须与同索引问题的题型匹配：`multi-select` 使用非空 `answers`，其他题型使用 `answer`，不能同时传两者。题目数量、数值类型、文本非空以及未开启自由输入时的选项值也会校验；`id` 可省略，携带时只用于审计，仍按顺序配对。

例如向多选题错误提交 `answer`：

```bash
curl -X POST "$BASE_URL/api/submit" \
  -H "Content-Type: application/json" \
  -d '{"agentKey":"default_agent","runId":"replace-me","awaitingId":"tool_question","params":[{"id":"q1","answer":"Weekend"},{"id":"q2","answer":"产品更新"}]}'
```

接口仍返回 HTTP 200，但响应中应为 `data.accepted:false`、`data.status:"invalid"`；等待项保持 awaiting，可将第二项改为 `{"id":"q2","answers":["产品更新"]}` 后重新提交。

approval:

```bash
curl -X POST "$BASE_URL/api/submit" \
  -H "Content-Type: application/json" \
  -d '{"agentKey":"default_agent","runId":"replace-me","awaitingId":"await_tool_bash","params":[{"id":"tool_bash","decision":"approve_rule_run","reason":"同规则本轮一并放行"}]}'
```

form:

```bash
curl -X POST "$BASE_URL/api/submit" \
  -H "Content-Type: application/json" \
  -d '{"agentKey":"default_agent","runId":"replace-me","awaitingId":"await_tool_bash","params":[{"id":"form-1","payload":{"applicant":{"name":"Lin","department":"engineering","employee_id":"E1001"},"leave_type":"事假","start_date":"2026-04-21","end_date":"2026-04-22","duration_days":2,"reason":"family_trip","urgent_contact":"Amy","urgent_phone":"13800138000","backup_person":"E2001","notes":"请协助处理审批"}}]}'
```

整批取消：

```bash
curl -X POST "$BASE_URL/api/submit" \
  -H "Content-Type: application/json" \
  -d '{"agentKey":"default_agent","runId":"replace-me","awaitingId":"tool_question","params":[]}'
```

```bash
curl -X POST "$BASE_URL/api/steer" \
  -H "Content-Type: application/json" \
  -d '{"agentKey":"default_agent","runId":"replace-me","message":"换个角度总结"}'
```

steer 被 run 接受并写入后，检查 `GET /api/chat/jsonl?chatId=<chatId>`：对应物理行必须是 `_type:"steer"` + 顶层 `steer` object，不得出现顶层 `event`；`steer` 内不得出现 `type/timestamp/seq/liveSeq`。随后检查 `GET /api/chat`，回放事件仍必须是扁平的 `type:"request.steer"`，并携带顶层 `timestamp` 与 `liveSeq`。

```bash
curl -X POST "$BASE_URL/api/interrupt" \
  -H "Content-Type: application/json" \
  -d '{"agentKey":"default_agent","runId":"replace-me"}'
```

Team-owned run 控制只传 `teamId`：

```bash
curl -N -X GET "$BASE_URL/api/attach?runId=replace-team-run&teamId=research&lastSeq=0"
```

```bash
curl -X POST "$BASE_URL/api/submit" \
  -H "Content-Type: application/json" \
  -d '{"teamId":"research","runId":"replace-team-run","awaitingId":"replace-task-id:replace-awaiting-id","params":[]}'
```

```bash
curl -X POST "$BASE_URL/api/steer" \
  -H "Content-Type: application/json" \
  -d '{"teamId":"research","runId":"replace-team-run","message":"优先给出可执行结论"}'
```

```bash
curl -X POST "$BASE_URL/api/interrupt" \
  -H "Content-Type: application/json" \
  -d '{"teamId":"research","runId":"replace-team-run"}'
```

把隐藏协调器 key 或任意 `agentKey` 与 Team-owned run 一起提交应返回 400；使用错误 `teamId` 应返回 403。

## Team Automation

创建 Team automation 时只传 `teamId`：

```bash
curl -X POST "$BASE_URL/api/automation/create" \
  -H "Content-Type: application/json" \
  -d '{"name":"Team 周报","description":"由 Team 编排周报","cron":"0 9 * * 1","teamId":"research","enabled":false,"query":{"message":"生成本周研究周报并完成复核"}}'
```

在同一 payload 中增加 `"agentKey":"writer"` 应校验失败。

## Learn

```bash
curl -X POST "$BASE_URL/api/learn" \
  -H "Content-Type: application/json" \
  -d '{"requestId":"learn_01","chatId":"replace-me"}'
```

## Agent Workspace File

读取当前 agent workspace 内的 Markdown / 文本文件；`path` 可以是相对 workspace 的路径，也可以是仍位于 workspace 内的绝对路径：

```bash
curl -X GET "$BASE_URL/api/file?agentKey=replace-agent&path=docs%2FREADME.md"
```

PDF、图片或下载预览使用同一个接口追加 `response=content`：

```bash
curl -L -X GET "$BASE_URL/api/file?agentKey=replace-agent&path=docs%2Fmanual.pdf&response=content" -o /tmp/manual.pdf
```

WebSocket 可读取 metadata 和文本内容；例如 JavaScript、Go、Markdown 等可解码文本会出现在响应的 `data.content`：

```json
{"frame":"request","type":"/api/file","id":"file_01","payload":{"agentKey":"replace-agent","path":"webpack.config.js"}}
```

WS 不传文件字节：二进制文件响应只包含 metadata 与 `contentUrl`；`response: "content"` 会返回 `400 invalid_request`，应改用 HTTP `GET /api/file?...&response=content`。文本读取上限由 `tools.yml` 的 `file-tools.max-read-bytes` 控制，默认 1 MiB，超出时响应标记 `truncated: true`。

## Project 文件浏览器 API

列出 CODER/KBASE Workspace 根目录和子目录；`path` 只能是相对 POSIX 路径：

```bash
curl -sS "$BASE_URL/api/project/tree?agentKey=replace-agent&limit=200"
curl -sS "$BASE_URL/api/project/tree?agentKey=replace-agent&path=internal%2Fserver&limit=200"
```

当根目录超过一页时，把第一次响应的 `nextCursor` 原样 URL 编码后继续请求。分页间新建、删除或改名文件应返回 409 `directory_changed`，客户端应丢弃该目录分页并从第一页重载。补充验证绝对路径、`..`、反斜杠、ChatsRoot、目录 symlink 和逃逸 symlink；它们不得被展开或越界读取。

读取某个 Chat 的 Run 文件变更；不传 `runId` 时保留各 Run 的独立记录：

```bash
curl -sS "$BASE_URL/api/project/changes?agentKey=replace-agent&chatId=replace-chat&limit=1000"
curl -sS "$BASE_URL/api/project/changes?agentKey=replace-agent&chatId=replace-chat&runId=replace-run"
```

读取单文件两侧文本快照：

```bash
curl -sS "$BASE_URL/api/project/diff?agentKey=replace-agent&chatId=replace-chat&runId=replace-run&path=internal%2Fserver%2Fproject.go"
```

用错误 Agent/Chat/Run 组合测试 owner 边界；二进制历史应返回 415，任一侧超过 `file-tools.max-read-bytes` 应返回 413。通过 Bash 或外部程序修改但未经过 `file_write/file_edit` 的文件只能在 `/api/file` 看见实时内容，changes/diff 不应伪造记录。

## Upload / Resource

```bash
curl -X POST "$BASE_URL/api/upload" \
  -F "requestId=upload_01" \
  -F "chatId=replace-me" \
  -F "file=@./README.md"
```

上传成功后，`data.upload.path` 是智能体可读的执行路径；使用响应中的 `data.upload.url` 访问平台资源：

```bash
curl -X GET "$BASE_URL/api/resource?file=replace-me%2FREADME.md"
```

如果部署开启了 resource ticket，需要把 chat 详情或其他接口返回的 ticket 透传到 `t` 参数：

```bash
curl -X GET "$BASE_URL/api/resource?file=replace-me%2FREADME.md&t=replace-me-ticket"
```

说明：

- `/api/file` 面向 agent workspace 文件预览；`/api/resource` 面向 chat 上传资源
- chat 上传资源的浏览器/普通客户端文件字节走 `POST /api/upload` 与 `GET /api/resource`
- 新生成/发布结果中的 `url` 是 `<chatId>/<relativePath>` 逻辑引用，客户端把它放入 `file` query；历史正文中的 `/api/resource?file=...` 仍应原样可读
- 用中文、空格、`#`、`%` 文件名重复测试，必须由标准 URL API 编码后读取，不手写 `%xx`；图片响应应保持原始字节、准确 MIME 和 `Content-Disposition: inline`
- 带合法 Bearer 和 `access_token` Cookie 分别读取当前用户资源；其他用户、错误 chatId、绝对路径、`..`、编码后的 traversal 和 symlink escape 必须失败
- `artifact_publish.path` 可使用当前 Workspace/Chat 内的相对路径、语义路径或 Host 绝对路径；受控根之外、其他 chat、`file://`、HTTP(S)、UNC 或当前主机不支持的 Windows 绝对路径必须失败
- WebSocket `/api/upload` 仅用于网关中转 `url + metadata`，由 platform 自己下载文件，不支持直接在 WS payload 中放 base64/二进制内容

## Viewport

question / approval 使用 builtin dialog，不必单独取 viewport；只有 `form` 需要 HTML viewport：

```bash
curl -X GET "$BASE_URL/api/viewport?viewportKey=leave_form"
```
